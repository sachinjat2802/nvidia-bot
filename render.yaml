services:
  - type: web
    name: moonu-bot
    runtime: docker # Changed to docker to match your Render logs
    plan: free
    region: singapore
    envVars:
      - key: NVIDIA_API_KEY
        sync: false
      - key: NVIDIA_BASE_URL
        value: https://integrate.api.nvidia.com/v1
      - key: DEFAULT_MODEL
        value: meta/llama-3.1-70b-instruct
      - key: CHAT_HISTORY_LIMIT
        value: 50
      - key: PORT
        value: 10000
    healthCheckPath: /
    autoDeploy: true
